---
  - import_tasks: "{{ role_path }}/tasks/adguard.yml"
    tags:
      - never
      - setup
      - restore
  - import_tasks: "{{ role_path }}/tasks/nginx_proxy_manager.yml"
    tags:
      - never
      - setup
      - restore
  - import_tasks: "{{ role_path }}/tasks/portainer.yml"
    tags:
      - never
      - setup
      - restore
  - import_tasks: "{{ role_path }}/tasks/teamspeak.yml"
    tags:
      - never
      - setup
      - restore
  - import_tasks: "{{ role_path }}/tasks/vaultwarden.yml"
    tags:
      - never
      - setup
      - restore
  - import_tasks: "{{ role_path }}/tasks/watchtower.yml"
    tags:
      - never
      - setup
      - restore
